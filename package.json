{
  "name": "babel-plugin-transform-switch-with",
  "version": "1.0.0",
  "description": "Compile switch-with statement and expressions.",
  "repository": "https://github.com/pj/babel-plugin-transform-switch-with",
  "license": "MIT",
  "main": "lib/index.js",
  "keywords": [
    "babel-plugin"
  ],
  "scripts": {
    "test": "npm run build && npm run lint && mocha",
    "test:basic": "mocha --ui tdd",
    "lint": "kcheck",
    "build": "babel src --out-dir lib",
    "watch": "babel src --out-dir lib --watch"
  },
  "dependencies": {
    "babel-plugin-syntax-switch-with": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-helper-plugin-test-runner": "^6.0.0",
    "babel-preset-es2015": "^6.6.0",
    "kcheck": "^2.0.2",
    "mocha": "^2.4.5"
  }
}
